# Core dependencies
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
langgraph>=0.2.0

# HTTP server for A2A endpoints
fastapi>=0.115.0
uvicorn>=0.32.0

# CapiscIO — langchain-capiscio wraps the SDK with LangChain-native APIs
# For local dev (not yet on PyPI):
#   pip install -e ../../../capiscio-langchain
# Once published:
#   langchain-capiscio>=0.1.0
capiscio-sdk>=2.3.0

# Shared event emitter (install from local path)
# Run: pip install -e ../../shared

# HTTP client for event emission
httpx>=0.27.0

# Environment management
python-dotenv>=1.0.0

# Optional: Web search tool
duckduckgo-search>=6.3.0
